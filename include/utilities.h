#include <stdio.h>
#include <string.h>

gboolean *file_exists(gchar *filename);

gchar *redland_XML_no_preamble(gchar *pstr);
gchar *space_deleter(char *punt);
gchar *space_deleter_construct(char *punt);


